@model OmdbSearchResult
<h1>Omdb</h1>
<ul>
    @foreach (OmdbResult result in Model.Search)
    {
        <li>
            @result.Title (@result.Year)
            &nbsp;
            @Html.ActionLink("Détails","Details","Omdb",new {result.imdbID})
        </li>
    }
</ul>
